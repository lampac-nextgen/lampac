enable: true
displayindex: 231
displayname: 24rolika
rch_access: apk
stream_access: apk
streamproxy: true
host: https://w2.huyalkino.com
priorityBrowser: http
headers_stream:
  referer: "{host}/"

menu:
  categories:
    format: "{cat}/page/{page}/"
    С русской озвучкой: russian
    Русское порно: russia
    Анал: gopa
    Ретро: retro
    Азиатки: asian-girl
    БДСМ: bdsm
    Большие члены: big-cock
    Большие сиськи: big-tits
    Групповуха: group
    Лесбиянки: lesbi
    Молодые девушки: teen
    Женская мастурбация: solo
    Красивый секс: beautiful
    Межрасовое: black
    Домашнее: homemade
    Ютуб: youtube-porno
    Одноклассники: ok
    Инцест: incest
    Оргазмы: orgasms

list:
  uri: page/{page}/

search:
  uri: index.php?do=search&subaction=search&search_start={page}&full_search=0&story={search}

model:
  format: "{host}/{(page > 1 ? $\"page/{page}/\" : string.Empty)}{model}"
  total_pages: 777

contentParse:
  eval: |
    if (Regex.IsMatch(html, "(?:/page/\\d+/)?xfsearch/actors/", RegexOptions.IgnoreCase))
    {
      pl.model = null;
      return pl;
    }

    if (pl.model == null)
    {
      try
      {
        string href = row.SelectSingleNode(".//a[@class='th-title']")?.GetAttributeValue("href", null)
          ?? row.SelectSingleNode(".//a")?.GetAttributeValue("href", null);

        if (!string.IsNullOrEmpty(href))
        {
          if (!href.StartsWith("http"))
            href = href.StartsWith("/") ? $"{init.host}{href}" : $"{init.host}/{href}";

          var cache = Shared.Services.Hybrid.HybridCache.GetMemory();
          string memKey = $"24rolika:model:v1:{href}";
          object cached;

          if (cache.TryGetValue(memKey, out cached) && cached is ModelItem modelItem)
          {
            pl.model = modelItem;
          }
          else
          {
            pl.myarg = $"model_probe:nexthub/model?plugin={Shared.Services.Utilities.AesTo.Encrypt(plugin)}&href={Shared.Services.Utilities.AesTo.Encrypt(href)}";
          }
        }
      }
      catch { }
    }

    return pl;
  nodes: //div[@class='thumb']
  name:
    node: .//a[@class='th-title']
  href:
    node: .//a
    attribute: href
  img:
    node: .//img
    attribute: data-src
  duration:
    node: .//div[@class='th-time icon-l']

view:
  bindingToIP: true
  related: true 
  viewsource: true
  regexMatch:
    pattern: 'file:"([^"]+)"'
