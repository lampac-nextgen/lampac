enable: true
displayindex: 330
displayname: Jopa Online
rch_access: apk,cors
stream_access: apk,cors
rchstreamproxy: web
host: https://jopaonline.mobi
priorityBrowser: http

menu:
  route:
    sort: "{host}/{sort}/{page}"
    cat: "{host}/categories/{cat}/{page}"
    catsort: "{host}/categories/{cat}/{sort}/{page}"
    model: "{host}/{model}{(page > 1 ? $\"/{page}\" : string.Empty)}"
    modelsort: "{host}/{model}/{sort}{(page > 1 ? $\"/{page}\" : string.Empty)}"
    -: "{url}"
  sort:
    Новинки: ""
    Топ рейтинга: toprated
    Популярнаe: popular
  categories:
    Мамки: mamki
    Русское: russkoe
    Жесткое: zhestkoe
    Зрелые: zrelye
    Измена: izmena
    Красотки: krasotki
    Домашнее: domashnee
    Большие члены: big-cock
    Групповое: gruppovoe
    Анал: anal
    Студенты: studenty
    Азиатки: asian
    Красивый секс: krasiviy-seks
    Большие сиськи: bolshie-siski
    Лесбиянки: lesbiyanki
    Жопы: zhopy
    Двойное проникновение: dvoynoe-proniknovenie
    Молодые: molodye
    Пикап: pickap
    Мастурбация: masturbation
    В ванной: v-vannoi
    Негры: s-negrami
    Мулатки: mulatki
    Худые: hudenkie
    Чулки: stockings
    Раком: rakom
    Минет: minet
    Рыжие: redhead
    Блондинки: blonde
    Брюнетки: bryunetki
    Межрасовое: mejrassovyy

list:
  uri: "{page}"

search:
  uri: search/{search}/{page}

model:
  format: "{host}/{model}{(page > 1 ? $\"/{page}\" : string.Empty)}"
  total_pages: 777

contentParse:
  eval: |
    string currentHeading = System.Text.RegularExpressions.Regex.Match(
      html,
      "<h1[^>]*>([^<]+)</h1>",
      System.Text.RegularExpressions.RegexOptions.IgnoreCase | System.Text.RegularExpressions.RegexOptions.Singleline
    ).Groups[1].Value?.Trim();

    if (pl.model == null)
    {
      try
      {
        string href = row.SelectSingleNode(".//a")?.GetAttributeValue("href", null);

        if (!string.IsNullOrEmpty(href))
        {
          if (!href.StartsWith("http"))
            href = href.StartsWith("/") ? $"{init.host}{href}" : $"{init.host}/{href}";

          var cache = Shared.Services.Hybrid.HybridCache.GetMemory();
          string memKey = $"jopaonline:model:v1:{href}";
          object cached;

          if (cache.TryGetValue(memKey, out cached) && cached is ModelItem modelItem)
          {
            if (string.IsNullOrEmpty(currentHeading) || currentHeading.IndexOf(modelItem.name, System.StringComparison.OrdinalIgnoreCase) < 0)
              pl.model = modelItem;
          }
          else
          {
            pl.myarg = $"model_probe:nexthub/model?plugin={Shared.Services.Utilities.AesTo.Encrypt(plugin)}&href={Shared.Services.Utilities.AesTo.Encrypt(href)}";

            if (!string.IsNullOrEmpty(currentHeading))
              pl.myarg += $"&current={Shared.Services.Utilities.AesTo.Encrypt(currentHeading)}";
          }
        }
      }
      catch { }
    }

    return pl;
  nodes: //div[@class='th']
  name:
    node: .//p
  href:
    node: .//a
    attribute: href
  img:
    node: .//img
    attribute: src
  duration:
    node: .//div[@class='th-duration']
  preview:
    node: .//img
    attribute: data-preview

view:
  related: true
  viewsource: true
  regexMatch:
    matches: ["720p", "480p", "360p", "240p"]
    pattern: '\[{value}\](https://[^''",\n\r\t ]+)'
