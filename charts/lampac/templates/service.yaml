apiVersion: v1
kind: Service
metadata:
  name: {{ include "lampac.fullname" . }}
  labels:
    {{- include "lampac.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lampac.selectorLabels" . | nindent 4 }}
